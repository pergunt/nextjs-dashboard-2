import {
  Shadows_Into_Light_Two,
  Gluten,
  Lusitana
} from 'next/font/google';

export const shadowsIntoLightTwo = Shadows_Into_Light_Two({
  subsets: ['latin'],
  weight: "400",
  variable: '--shadows-into-light-2'
});

export const gluten = Gluten({
  subsets: ['latin'],
  weight: "400",
  variable: '--gluten'
});

export const lusitana = Lusitana({
  subsets: ['latin'],
  weight: "400",
  variable: '--lusitana'
});
